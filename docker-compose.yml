services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./backend/serviceAccountKey.json:/app/serviceAccountKey.json

  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend
